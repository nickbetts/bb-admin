// TODO: Implement Clickr layout — see src/app/(clickr)/CLICKR_PLAN.md § Phase 6
// Minimal nav: Clickr logo, Login link, Sign Up button.
// No Stratos sidebar — this is the public-facing product shell.

export default function ClickrLayout({ children }: { children: React.ReactNode }) {
  return children;
}
