"use client";
// TODO: Implement Clickr LP detail page — see CLICKR_PLAN.md § Phase 6
// Sections:
//   - Preview iframe (src="/lp/{publicSlug}")
//   - Edit brief → re-generate (confirm prompt)
//   - Trigger audit pass → show audit findings
//   - Share link panel (publicSlug URL, copy button)
//   - Publish/unpublish toggle
//   - Upgrade prompt if on free tier

export default function ClickrPageDetailPage() {
  return (
    <main style={{ fontFamily: "sans-serif", padding: "40px 20px" }}>
      <h1>Landing page</h1>
      <p>TODO: implement LP detail page</p>
    </main>
  );
}
